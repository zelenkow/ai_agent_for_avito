# AI Agent for Avito: Анализ качества коммуникаций в чатах

Telegram-бот для автоматического анализа переписки с клиентами на Avito с использованием AI (DeepSeek) для оценки качества коммуникации менеджеров.


## О проекте

Проект представляет собой интеллектуального Telegram-бота, который автоматически синхронизирует данные чатов с платформы Avito, анализирует диалоги с помощью AI-модели DeepSeek и формирует детальные отчеты о качестве коммуникации менеджеров.


**Цель проекта:** Повышение эффективности работы менеджеров по продажам за счет автоматического анализа их коммуникации с клиентами и выявления областей для улучшения.


**Ключевые особенности:**
- Автоматическая ежедневная синхронизация чатов и сообщений с Avito
- AI-анализ тональности, профессионализма и эффективности диалогов
- Гибкая система отчетности с различными периодами
- Удобный Telegram-интерфейс для просмотра отчетов
- Ежедневная автоматическая рассылка отчетов


## Анализируемые параметры

AI-модель оценивает каждый диалог по следующим критериям:

- **Тональность коммуникации** – эмоциональная окраска сообщений
- **Профессионализм менеджера** – соответствие стандартам делового общения
- **Ясность изложения информации** – понятность и структурированность сообщений
- **Решение проблем клиента** – эффективность ответов на вопросы и решение проблем
- **Работа с возражениями** – умение работать с возражениями клиентов
- **Завершение диалога** – корректность завершения коммуникации


##  Архитектура и стек технологий

### Backend
- **Python** – основной язык разработки
- **Aiogram** – асинхронный фреймворк для Telegram ботов
- **APScheduler** – планировщик задач для автоматических операций
- **AsyncPG** – асинхронный драйвер для PostgreSQL
- **AIOHTTP** – асинхронные HTTP-запросы к API

### База данных
- **PostgreSQL 18** – реляционная база данных
- **Yoyo Migrations** – система миграций базы данных

### AI/ML
- **DeepSeek API** – LLM для анализа диалогов
- **CacheTools** – кэширование токенов API

### Инфраструктура
- **Docker & Docker Compose** – контейнеризация и оркестрация
- **PostgreSQL (Docker)** – база данных в контейнере

### Внешние API
- **Avito API** – получение данных о чатах и сообщениях
- **Telegram Bot API** – взаимодействие с пользователями

## Режимы работы

Проект поддерживает несколько режимов работы, которые можно запускать через аргумент `--command`:

### 1. **Polling режим**
Запускает Telegram-бота в режиме long-polling
```bash
python src/main.py --command polling
```

### 2. **Avito синхронизация**
Только синхронизация данных с Avito (без запуска бота).
```bash
python src/main.py --command avito
```

### 3. **AI анализ**
Только анализ диалогов с помощью DeepSeek (без синхронизации и бота).
```bash
python src/main.py --command llm
```

### 4. **Отправка отчетов по таймеру**
Ручной запуск отправки ежедневных отчетов.
```bash
python src/main.py --command timer
```
### 5. **Webhook режим**
Основной режим бота, синхронизация данных, ии анализ
и отчеты работают в автоматическом режиме. 
Запуск через api.py на серверах.

##  Быстрый старт

### Предварительные требования
- Python 3.11 или выше
- Docker и Docker Compose
- Аккаунт на Avito с доступом к API
- API ключ DeepSeek
- Telegram бот (создать через @BotFather)

### Установка и запуск

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd ai_agent_for_avito
```

2. **Создание виртуального окружения**
```bash
python -m venv venv
source venv/bin/activate
```

3. **Установка зависимостей**
```bash
pip install -r requirements.txt
```

4. **Настройка переменных окружения**
```bash
cp example.env .env
# Отредактируйте .env файл, заполнив все необходимые значения
```

5. **Запуск базы данных**
```bash
docker-compose up -d postgres
```

6. **Применение миграций**
```bash
yoyo apply --database postgresql://postgres:password@localhost/ai_agent ./migrations
```

7. **Запуск бота**
```bash
python src/main.py --command polling
```

## Настройка окружения

Создайте файл `.env` на основе `example.env` и заполните следующие переменные:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен вашего Telegram бота от @BotFather | `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11` |
| `AVITO_CLIENT_ID` | Client ID из личного кабинета Avito для разработчиков | `abc123def456` |
| `AVITO_CLIENT_SECRET` | Client Secret из личного кабинета Avito для разработчиков | `secret123456` |
| `AVITO_USER_ID` | ID пользователя Avito (номер аккаунта) | `123456789` |
| `DEEPSEEK_API_KEY` | API ключ для DeepSeek | `sk-1234567890abcdef` |
| `PG_HOST` | Хост PostgreSQL | `localhost` |
| `PG_PORT` | Порт PostgreSQL | `5432` |
| `PG_DATABASE` | Имя базы данных | `ai_agent` |
| `PG_USER` | Пользователь PostgreSQL | `postgres` |
| `PG_PASSWORD` | Пароль PostgreSQL | `password` |
| `APIKEY` | Секретный ключ для доступа к API вашего Telegram бота | `your_secret_key` |
| `WEBHOOK_URL` | Адрес сервера (опционально) | `https://your-domain.com` |

## Использование

### Команды Telegram бота

- `/start` – Запустить бота и ознакомиться с возможностями
- `/report` – Сформировать отчет за выбранный период
- `/help` – Показать справку по использованию бота
- `/cancel` – Отменить текущую операцию

### Получение отчетов

1. Отправьте команду `/report` боту
2. Выберите период из предложенных вариантов:
   - 📅 За день
   - 📅 За неделю
   - 📅 За месяц
   - 📅 Свой период
3. Для собственного периода введите даты в формате `ДД.ММ.ГГГГ`
4. Просматривайте отчеты с помощью навигационных кнопок

### Автоматические отчеты

Бот автоматически отправляет ежедневные отчеты каждый день в **10:00 по московскому времени**. Отчеты содержат анализ всех диалогов за предыдущий день.

##  Структура проекта

```
ai_agent_for_avito/
├── src/                    # Исходный код
│   ├── main.py            # Основной файл приложения
│   ├── avito.py           # Работа с API Avito
│   ├── llm.py             # Интеграция с DeepSeek API
│   ├── database.py        # Работа с базой данных
│   ├── utils.py           # Вспомогательные функции
│   └── retry_config.py    # Конфигурация повторных попыток
├── migrations/            # Миграции базы данных
│   └── 001_initial_schema.sql
├── docs/                  # Документация
│   ├── Agent.pptx         # Презентация проекта
│   ├── final_requirements_zelenkow.pdf  # Требования
│   └── child_artefacts/   # Артефакты
├── docker-compose.yml     # Конфигурация Docker
├── requirements.txt       # Зависимости Python
├── example.env           # Пример переменных окружения
└── README.md             # Этот файл
```

##  Документация

В папке `docs/` находится полная документация по проекту:

- **`Agent.pptx`** – Презентация проекта
- **`final_requirements_zelenkow.pdf`** – Детальные требования к системе
- **`child_artefacts/`** – Артефакты:
  - Модель данных AI
  - Acceptance Criteria и NFR
  - Activity диаграммы
  - Архитектурные диаграммы
  - ERD диаграммы
  - REST спецификации
  - Sequence диаграммы
  - Swagger документация
  - User Case описания
  - User Story описания
  - Wireframe макеты


**Видеопрезентация:** [Ссылка на видеопрезентацию](https://vkvideo.ru/video800283590_456239017)

**Примечание:** Проект создан в образовательных целях и может требовать доработок для использования в production-среде.
